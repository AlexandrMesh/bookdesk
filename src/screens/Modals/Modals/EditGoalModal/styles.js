import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  wrapper: {
    maxWidth: 400,
    padding: 15,
  },
  submitButtonWrapper: {
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    margin: 15,
  },
});
