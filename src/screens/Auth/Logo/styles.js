import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  logoWrapper: {
    marginBottom: 50,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
  },
  logo: {
    width: 100,
    height: 100,
  },
});
